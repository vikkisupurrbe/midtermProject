<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Results</title>

    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" />
    <link rel="stylesheet" href="/vendor/border-box.css" />
    <link rel="stylesheet" href="/styles/header.css" />
    <link rel="stylesheet" href="/styles/layout.css" />

    <script src="/vendor/jquery-3.0.0.js"></script>
    <script defer src="/scripts/share_results.js"></script>
    <% if (results !== null) { %>
    <script>const quizURL = `/quizzes/attempt/<%= results.quiz_id %>` </script>
    <% }; %>
  </head>

  <body>
    <%- include('partials/header') %>
    <% if (results === null) { %>
      <span> Results not available! </span>
    <%} else {%>
    <main>
    <h1>Here are your results for</h1>
    <h3><label id="title"><%= results.title %></label></h3x>
    <label id="name"><%= results.name %></label>
    <label id="correct-answers"><%= results.correct_answers %></label>/
    <label id="total-questions"><%= results.total_questions %></label>

    <button id="share-results">Share my results</button>
  </main>
  </body>
  <footer><button id="share-quiz">Share this quizz!</button></footer>
  <%} %>
</html>
